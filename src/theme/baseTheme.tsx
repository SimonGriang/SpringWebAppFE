// src/theme/baseTheme.ts
import { createTheme } from "@mantine/core";

export const baseTheme = createThfeme({
  fontFamily: "Inter, system-ui, sans-serif",
  radius: "md",
  spacing: { md: 12 },
});
